import { Outlet } from "react-router-dom";

const AuthLayouts = () => {
    return (
        <div>
              AuthLayouts
              <Outlet></Outlet>
        </div>
    );
};

export default AuthLayouts;